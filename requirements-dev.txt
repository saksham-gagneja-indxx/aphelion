# Development and test dependencies.
#
# Kept separate from requirements.txt so deployments never build them - on a
# constrained free-tier instance every avoided package is build time and memory
# we get back.
#
# Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-cov==4.1.0
