# ============ ENVIRONMENT ============
.env
.env.local
.env.*.local
# Backups written by `python -m backend.admin_cli` before it edits .env. These
# are full copies of the file, secrets and all — ignoring only .env would leave
# them committable.
.env.bak
.env.*.bak
.env.backup*
.venv
venv/
ENV/
env/

# ============ PYTHON ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# ============ TESTING ============
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# ============ IDE ============
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.sublime-project
.sublime-workspace

# ============ LOGS ============
*.log
logs/
data/logs/

# ============ DATA ============
data/reels/*
!data/reels/.gitkeep
data/uploads/*
!data/uploads/.gitkeep
data/*.db
# WAL companions. SQLite runs in WAL mode now, so these sit beside the database
# and are meaningless without it.
data/*.db-wal
data/*.db-shm
data/cache/

# ============ MEDIA ============
*.mp4
*.mov
*.avi
*.mkv
*.webm
*.jpg
*.jpeg
*.png
*.gif

# ============ DOCKER ============
.dockerignore
.docker/

# ============ MISC ============
.env.db
*.pyc
*.pyo
*.pyd
.mypy_cache/
.dmypy.json
dmypy.json
node_modules/
.next/

# ============ CLOUDFLARE WORKERS (mcp-server/) ============
.wrangler/
.dev.vars
.dev.vars.*
