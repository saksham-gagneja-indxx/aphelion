# ============================================
# Social Media Automation - 24h Sprint Deps
# ============================================
# Trimmed from requirements.txt to only what backend/ actually imports.
# Cut per docs/TIMELINE.md "What Got Cut": LinkedIn, AI (anthropic/nltk),
# PostgreSQL (psycopg2), Alembic, pandas/numpy, sentry/loki, dev tooling.
# Full requirements.txt is kept for the post-deadline phases.
#
# Versions are floored (not pinned) where the original pin had no
# Python 3.13 wheel and had to build from source.

# ============ CORE FRAMEWORK ============
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0
Werkzeug==3.0.0

# ============ INSTAGRAM ============
instagrapi>=2.18.0         # 2.0.0 pinned pydantic==1.10.9, which conflicts
                           # with pydantic-settings (v2) used by utils/config.py
requests>=2.32.0           # floored to satisfy instagrapi's constraint

# ============ DATABASE ============
SQLAlchemy>=2.0.31          # 2.0.23 pin predates py3.13 support

# ============ MEDIA ============
Pillow>=11.0.0              # 10.1.0 pin has no py3.13 wheel

# ============ SCHEDULING ============
APScheduler==3.10.4
pytz==2023.3
python-dateutil==2.8.2

# ============ VALIDATION ============
pydantic>=2.9.0             # pydantic-core py3.13 wheels start at 2.9
pydantic-settings>=2.5.0

# ============ TESTING ============
pytest>=8.0.0
